#pragma once
#include "LSDB.h"

// This loads a the topology while starting the project.
// Now I don't have to seed the topology with seed_topology.sh script manually
void loadDefaultTopology(LSDB& lsdb);